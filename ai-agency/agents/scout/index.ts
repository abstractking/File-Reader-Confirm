// agents/scout/index.ts — STUB (build next)
import { Task, AgentRunResult } from "../../core/types";
export async function run(task: Task): Promise<AgentRunResult> {
  return {
    summary: "SCOUT agent not yet implemented",
    data: { status: "stub", task_type: task.task_type }
  };
}
